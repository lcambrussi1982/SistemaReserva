/**
 * usuario.js
 * Apenas delega para o auth.js preencher o perfil via localStorage.
 * Mantido separado caso queira adicionar ações extras (alterar senha, etc.).
 */

document.addEventListener("DOMContentLoaded", () => {
  // Toda a lógica principal está em auth.js para reaproveitar.
});
